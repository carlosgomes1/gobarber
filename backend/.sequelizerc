const { resolve } = require('path')

module.exports = {
    config: resolve('./src/config', 'database.js'),
    'models-path': resolve('./src/app/models'),
    'migrations-path': resolve('./src/database/migrations'),
    'seeder-path': resolve('./src/database/seeds')
}